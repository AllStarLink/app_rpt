name: AI disclosure (ASL003)
on:
  pull_request_target:
    types: [opened, edited, reopened, synchronize]
permissions:
  contents: read
  issues: write
  pull-requests: write
jobs:
  ai-disclosure:
    uses: AllStarLink/.github/.github/workflows/ai-disclosure.yml@main
