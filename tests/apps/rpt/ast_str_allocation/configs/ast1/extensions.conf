[general]

[default]

[globals]

[timer]
exten => start,1,NoOp(Starting ast_str allocation test)
    same => n,UserEvent(TimerExpired,TimerEvent:start)
    same => n,Wait(2)
    same => n,UserEvent(TimerExpired,TimerEvent:disconnect)
    same => n,Wait(2)
    same => n,Hangup()

[wait]
exten => s,1,Answer()
    same => n,Wait(20)
    same => n,Hangup()

[radio-control]
exten => _XXXX,1,Rpt(${EXTEN})