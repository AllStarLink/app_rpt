;
; Module configuration for HTTP registration failure testing
;

[modules]
autoload=no

; Core modules required
load => res_musiconhold.so
load => res_timing_timerfd.so
load => res_crypto.so

; Curl support
load => res_curl.so

; IAX2 support
load => chan_iax2.so

; Manager interface
load => manager.so

; DNS manager
load => res_dnsmgr.so